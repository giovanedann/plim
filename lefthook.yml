pre-commit:
  commands:
    lint-fix:
      glob: "*.{ts,tsx,js,jsx,json}"
      run: pnpm biome check --write --staged --no-errors-on-unmatched {staged_files}
      stage_fixed: true
      priority: 1
    typecheck:
      run: pnpm typecheck
      priority: 2
    lint:
      run: pnpm lint
      priority: 3
    test:
      run: pnpm test
      priority: 4

pre-push:
  commands:
    typecheck:
      run: pnpm typecheck
    lint:
      run: pnpm lint
    test:
      run: pnpm test
